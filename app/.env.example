# Copie para .env e preencha. NUNCA commitar o .env real.

# ─── Carteira Web3 (Privy) ───
# App ID do Privy (privy.io) — é público (vai no bundle do cliente).
VITE_PRIVY_APP_ID=""
# Modo da carteira: 'mock' (sandbox) | 'testnet' (Privy + deploy real). Ausente = mock.
VITE_WALLET_MODE="mock"

# ─── Co-pilot Gemini (opcional, server-side) ───
# Se vazia, o endpoint /api/copilot roda em modo simulação determinística.
# Obtenha em https://aistudio.google.com/apikey
GEMINI_API_KEY=""

# ─── Verificação automática do source na BaseScan (server-side) ───
# Chave da API V2 unificada da Etherscan (1 chave vale p/ todas as chains, incl. Base).
# Sem ela, o token lança normal, mas o source NÃO é verificado (badge "não verificado").
# Obtenha em https://etherscan.io/myapikey · configure também no painel da Vercel (env de produção).
ETHERSCAN_API_KEY=""

# ─── App URL ───
# Em dev, deixe vazio (Express usa http://localhost:3000).
APP_URL=""
